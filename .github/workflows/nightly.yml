name: nightly
on:
  schedule:
  - cron: "0 12 * * *"
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout sources
      uses: actions/checkout@v1
    - name: Set up Java 11
      uses: actions/setup-java@v1
      with:
        java-version: 11
    - name: Build with Maven
      run: mvn -B -DskipTests=true -f pom.xml clean install
    - name: Login to Docker Hub
      uses: azure/docker-login@v1
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
    - name: Build amd64 image
      run: |
        cd webapp
        docker build -f src/main/docker/Dockerfile.arm -t manorrock/munchkin:arm-snapshot .
        docker push manorrock/munchkin:arm-snapshot
    - name: Build arm image
      run: |
        cd webapp
        docker build -f src/main/docker/Dockerfile.arm -t manorrock/munchkin:arm-snapshot .
        docker push manorrock/munchkin:arm-snapshot
    - name: Push Docker manifest
      run: |
        cd webapp
        export DOCKER_CLI_EXPERIMENTAL=enabled
        docker manifest create manorrock/munchkin:snapshot \
          manorrock/munchkin:amd64-snapshot \
          manorrock/munchkin:arm-snapshot
        docker manifest annotate manorrock/munchkin:snapshot \
          manorrock/munchkin:amd64-snapshot --os linux --arch amd64
        docker manifest annotate manorrock/munchkin:snapshot \
          manorrock/munchkin:arm-snapshot --os linux --arch arm
        docker manifest push manorrock/munchkin:snapshot
